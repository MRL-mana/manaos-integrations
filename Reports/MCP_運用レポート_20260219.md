# MCP 運用レポート（2026-02-19）

## 概要
- Obsidian MCP と Google Drive MCP の実運用テストを完了。
- mcp-api コンテナ設定を更新し、Vault/認証情報を実運用パスへ接続。
- LLM Router は 5211 で安定稼働（OpenAI互換エンドポイント応答確認済み）。

## 実施変更
- `mcp_api_server.py`
  - Google Drive 統合初期化で環境変数経由の認証パスを使用:
    - `GOOGLE_DRIVE_CREDENTIALS`
    - `GOOGLE_DRIVE_TOKEN`
- `docker-compose.always-ready-llm.yml`
  - `mcp-api` に以下を反映:
    - `OBSIDIAN_VAULT_PATH=/app/obsidian_vault`（既定）
    - `GOOGLE_DRIVE_CREDENTIALS=/app/credentials.json`（既定）
    - `GOOGLE_DRIVE_TOKEN=/app/token.json`（既定）
    - `../:/app/obsidian_vault` マウント追加
    - `./credentials.json:/app/credentials.json:ro` マウント追加
    - `./token.json:/app/token.json`（書き込み可）に修正

## 検証結果
- Obsidian MCP
  - `obsidian_create_note` 成功
  - 生成ノートパス例: `/app/obsidian_vault/MCP_Obsidian_ProdTest_2026-02-19_150137.md`
- Google Drive MCP
  - `google_drive_upload` 成功
  - ファイルID例: `10SptkIOJHGD1tHmG0hvKNVNQJTplst79`
- ヘルス
  - `http://127.0.0.1:9502/health` -> 200
  - `http://127.0.0.1:9503/health` -> 200
- コンテナ状態
  - `mcp-api-server` healthy
  - `tool-server` healthy
  - `open-webui` healthy
- LLM Router
  - アクティブポート: 5211（`logs/manaos_llm_router_port.txt`）
  - `GET /v1/models` -> 200
  - `POST /v1/chat/completions` -> 応答成功（READY）

## Git
- 反映済みコミット: `777bebb`
- 内容: Obsidian/Drive MCP コンテナ運用対応（設定・マウント・認証パス）

## 既知事項
- `-Port 5111` 指定起動時は、5111使用中の場合に 5211 へ自動再利用フォールバックする。
- 現運用では 5211 が正常稼働中のため、機能上の問題なし。

## 運用方針（確定）
- LLM Router の標準ポートは `5211` とする。
- ワークスペースタスク `ManaOS: LLM Router起動（Auto Port）` は `-Port 5211` に更新済み。
