"""
Google Calendar・Tasks・Sheets・Keep API 権限设定ガイド
マナOS向けのGoogle生産性ツール統合が完全に動作するための設定手順
"""

import sys
from pathlib import Path

print("""
╔════════════════════════════════════════════════════════════════════════════╗
║                Google API スコープ設定ガイド                                  ║
║         Calendar・Tasks・Sheets・Keep の完全な権限を有効化する                  ║
╚════════════════════════════════════════════════════════════════════════════╝

現在のステータス:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Google Drive 統合: 完全動作中（ファイルアップロード・ダウンロード可能）
✅ Google Sheets 統合: 動作中（データ読み書き可能）
⚠️  Google Calendar: スコープ権限不足
⚠️  Google Tasks: スコープ権限不足
⚠️  Google Keep: スコープ権限不足

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 必要な設定手順:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【STEP 1】Google Cloud Console にアクセス
  🔗 https://console.cloud.google.com/

【STEP 2】プロジェクト選択
  • プロジェクト: "ManaOS-Integrations" または使用中のプロジェクト
  • 確認: コンソール左上に表示されている

【STEP 3】有効化する API
  
  🔵 Google Calendar API を有効化:
    1. 左メニュー → 「有効なAPIとサービス」
    2. 「API を有効化」ボタンをクリック
    3. 検索: "Google Calendar API"
    4. クリック → 「有効化」

  🔵 Google Tasks API を有効化:
    1. 同様に「API を有効化」
    2. 検索: "Google Tasks API"
    3. クリック → 「有効化」

  🔵 Google Keep API を有効化:
    1. 同様に「API を有効化」
    2. 検索: "Keep API" または "Google Keep API"
    3. クリック → 「有効化」

【STEP 4】OAuth 同意画面でスコープを追加（重要）
  
  1. 左メニュー → 「OAuth同意画面」
  2. 「編集」ボタンをクリック
  3. 「3. オプション設定（スコープ）」セクションまでスクロール
  4. 「スコープを追加または削除」ボタンをクリック
  5. 以下のスコープを検索して追加:

     📍 calendar.calendar:
       https://www.googleapis.com/auth/calendar

     📍 tasks.tasks:
       https://www.googleapis.com/auth/tasks

     📍 keep.readonly:
       https://www.googleapis.com/auth/keep.readonly

   6. 「更新」をクリック
   7. 「保存」をクリック

【STEP 5】ローカル認証トークンを再生成（自動）
  
  token.json は既に削除されました。
  次回スクリプト実行時に、新しいスコープで自動的に再認証されます。
  
  ブラウザが開いて Google ログインフォームが表示されたら:
  1. Google アカウントでログイン
  2. ManaOS に権限させることを許可

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 トラブルシューティング:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❓ 「Insufficient Permission」エラーが出る
  → OAuth 同意画面でスコープが正しく追加されていないかもしれません
  → token.json を再度削除して、新スコープで再認証してください

❓ ブラウザが開かない
  → ローカルホスト認証に失敗している可能性があります
  → ファイアウォール設定を確認してください
  → または credentials.json の redirect_uri を確認

❓ "API is not enabled"エラー
  → Google Cloud Console で該当 API が有効化されていません
  → STEP 3 を確認してください

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 完了後に期待される機能:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Google Calendar:
   • イベント一覧取得
   • イベント作成・編集・削除
   • リマインダー設定

✅ Google Tasks (マナの TODO管理):
   • タスク一覧表示
   • タスク追加・編集
   • 完了マーク・削除

✅ Google Sheets (スプレッドシート連携):
   • データの読み書き
   • グラフ出力・分析

✅ Google Keep (メモ同期):
   • メモ閲覧・検索

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔑 現在の認証情報:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

credentials.json の場所:
  📂 C:\\Users\\mana4\\Desktop\\manaos_integrations\\credentials.json

token.json (認証トークン):
  ✅ 削除済み - 次回再認証時に再生成されます

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 設定完了後:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

以下のコマンドで再度テストしてください:

  python .\\test_google_productivity.py

すべての機能が「✅」になれば設定完了です！

╔════════════════════════════════════════════════════════════════════════════╗
║                          設定を開始してください                                ║
╚════════════════════════════════════════════════════════════════════════════╝
""")

print("\n⏳ 設定が完了したら、このコマンドを実行してください:")
print("   python .\\test_google_productivity.py\n")
