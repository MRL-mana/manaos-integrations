name: VSCode設定検証

on:
  push:
    branches: [ master, main, develop ]
    paths:
      - '.vscode/**'
      - 'validation_script.ps1'
      - '.github/workflows/vscode-validation.yml'
  pull_request:
    branches: [ master, main ]
    paths:
      - '.vscode/**'
      - 'validation_script.ps1'
      - '.github/workflows/vscode-validation.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-vscode-setup:
    name: VSCode設定ファイル検証
    runs-on: windows-latest
    continue-on-error: true
    
    steps:
    - name: チェックアウト
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 変更種別の判定
      id: changed
      uses: dorny/paths-filter@v3
      with:
        filters: |
          vscode:
            - '.vscode/**'
          validator:
            - 'validation_script.ps1'
          workflow:
            - '.github/workflows/vscode-validation.yml'
          scripts:
            - 'start_vscode_cursor_services.py'
            - 'autonomous_operations.py'
            - 'check_services_health.py'
            - 'emergency_stop.py'
      
    - name: Python環境セットアップ
      if: steps.changed.outputs.validator == 'true' || steps.changed.outputs.scripts == 'true' || github.event_name == 'workflow_dispatch'
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
        
    - name: 依存関係インストール
      if: steps.changed.outputs.validator == 'true' || steps.changed.outputs.scripts == 'true' || github.event_name == 'workflow_dispatch'
      run: |
        python -m pip install --upgrade pip
        if (Test-Path requirements-core.txt) {
          pip install -r requirements-core.txt
        }
      shell: pwsh
      
    - name: 必須ファイル存在確認
      if: steps.changed.outputs.vscode == 'true' || steps.changed.outputs.validator == 'true' || github.event_name == 'workflow_dispatch'
      run: |
        $files = @(
          ".vscode/extensions.json",
          ".vscode/tasks.json",
          ".vscode/launch.json",
          ".vscode/settings.json",
          "VSCODE_SETUP_GUIDE.md",
          "VSCODE_CHECKLIST.md",
          "validation_script.ps1"
        )
        
        $missing = @()
        foreach ($file in $files) {
          if (-not (Test-Path $file)) {
            $missing += $file
            Write-Error "Missing file: $file"
          } else {
            Write-Host "✅ Found: $file" -ForegroundColor Green
          }
        }
        
        if ($missing.Count -gt 0) {
          Write-Error "Missing $($missing.Count) required files"
          exit 1
        }
      shell: pwsh
      
    - name: JSON構文検証
      if: steps.changed.outputs.vscode == 'true' || github.event_name == 'workflow_dispatch'
      run: |
        $jsonFiles = @(
          ".vscode/extensions.json",
          ".vscode/tasks.json",
          ".vscode/launch.json"
        )
        
        $errors = 0
        foreach ($file in $jsonFiles) {
          try {
            $content = Get-Content $file -Raw | ConvertFrom-Json -ErrorAction Stop
            Write-Host "✅ Valid JSON: $file" -ForegroundColor Green
          } catch {
            Write-Error "❌ Invalid JSON: $file - $($_.Exception.Message)"
            $errors++
          }
        }
        
        if ($errors -gt 0) {
          Write-Error "Found $errors JSON syntax errors"
          exit 1
        }
      shell: pwsh
      
    - name: Pythonコード構文確認
      if: steps.changed.outputs.validator == 'true' || steps.changed.outputs.scripts == 'true' || github.event_name == 'workflow_dispatch'
      run: |
        $pyFiles = @(
          "start_vscode_cursor_services.py",
          "autonomous_operations.py",
          "check_services_health.py",
          "emergency_stop.py"
        )
        
        $errors = 0
        foreach ($file in $pyFiles) {
          if (Test-Path $file) {
            python -m py_compile $file 2>&1
            if ($LASTEXITCODE -ne 0) {
              Write-Error "❌ Syntax error in: $file"
              $errors++
            } else {
              Write-Host "✅ Valid Python syntax: $file" -ForegroundColor Green
            }
          }
        }
        
        if ($errors -gt 0) {
          Write-Error "Found $errors Python syntax errors"
          exit 1
        }
      shell: pwsh
      
    - name: ドキュメントリンク検証
      if: steps.changed.outputs.validator == 'true' || github.event_name == 'workflow_dispatch'
      run: |
        $docs = @(
          "VSCODE_SETUP_GUIDE.md",
          "VSCODE_VS_CURSOR.md",
          "VSCODE_CHECKLIST.md",
          "VSCODE_TEST_CHECKLIST.md",
          "SNIPPETS_GUIDE.md",
          "QUICKREF.md",
          "STARTUP_GUIDE.md",
          "SYSTEM3_GUIDE.md",
          "EMERGENCY_STOP_GUIDE.md"
        )
        
        $broken = @()
        foreach ($doc in $docs) {
          if (-not (Test-Path $doc)) {
            Write-Warning "⚠️ Missing documentation: $doc"
            $broken += $doc
          } else {
            Write-Host "✅ Found: $doc" -ForegroundColor Green
          }
        }
        
        if ($broken.Count -gt 3) {
          Write-Error "Too many missing documentation files: $($broken.Count)"
          exit 1
        }
      shell: pwsh
      
    - name: スニペット検証
      if: steps.changed.outputs.vscode == 'true' || github.event_name == 'workflow_dispatch'
      run: |
        if (Test-Path ".vscode/python.code-snippets") {
          try {
            $snippets = Get-Content ".vscode/python.code-snippets" -Raw | ConvertFrom-Json
            $count = ($snippets | Get-Member -MemberType NoteProperty).Count
            Write-Host "✅ Found $count code snippets" -ForegroundColor Green
            
            if ($count -lt 5) {
              Write-Warning "⚠️ Only $count snippets found (expected at least 5)"
            }
          } catch {
            Write-Error "❌ Invalid snippets file: $($_.Exception.Message)"
            exit 1
          }
        } else {
          Write-Warning "⚠️ python.code-snippets not found"
        }
      shell: pwsh
      
    - name: 検証結果サマリー
      run: |
        Write-Host "`n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Cyan
        Write-Host " VSCode設定検証完了 ✅" -ForegroundColor Green
        Write-Host "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" -ForegroundColor Cyan
        Write-Host "`n✅ すべての検証に合格しました`n" -ForegroundColor Green
      shell: pwsh
